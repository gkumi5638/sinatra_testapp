# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# gem "rails"
gem 'sinatra'
gem 'sinatra-contrib' # reloader rbファイルの変更後に毎回起動する必要がなくなる
gem 'redcarpet' # markdown
